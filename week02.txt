리눅스 명령어 
ls
ls -a
touch 파일명
echo "출력할 문자열"
echo "출력할 문자열" >> 파일명
cat
cd ..
cd 폴더명


깃 명령어
git init
git status
git add 파일명
git add .
git config --global user.email "이메일주소"
git config --global user.name "필명(사용자이름)"
git config --list
git commit -m "커밋 메세지"
git log
git checkout 해시코드(16진수 전체 또는 7자리)
git checkout -
git branch -M main
git remote add origin 원격리포지토리주소
git push -u origin main
